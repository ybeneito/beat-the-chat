services:
  - type: web
    name: beat-the-chat-backend
    runtime: node
    plan: free
    buildCommand: pnpm install --frozen-lockfile && pnpm -w run render:build
    startCommand: pnpm --filter backend start
    healthCheckPath: /health

